<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>🎵 [민호특공대] 2025.10.14ver. Analog Vol.1 음원사이트별 원클릭</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", "Pretendard", Arial, sans-serif;
            background: linear-gradient(135deg, #4facfe 0%, #00f2fe 50%, #1dd1a1 100%);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
            position: relative;
            overflow: hidden;
        }

        body::before {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: 
                radial-gradient(circle at 20% 50%, rgba(29, 209, 161, 0.2) 0%, transparent 50%),
                radial-gradient(circle at 80% 80%, rgba(79, 172, 254, 0.2) 0%, transparent 50%);
            animation: pulse 4s ease-in-out infinite;
            pointer-events: none;
        }

        @keyframes pulse {
            0%, 100% { opacity: 0.5; }
            50% { opacity: 1; }
        }

        .container {
            max-width: 600px;
            width: 100%;
            background: rgba(255, 255, 255, 0.98);
            border-radius: 30px;
            box-shadow: 0 30px 80px rgba(0, 0, 0, 0.3);
            padding: 50px 40px;
            text-align: center;
            position: relative;
            z-index: 10;
            backdrop-filter: blur(10px);
        }

        .header {
            margin-bottom: 40px;
        }

        h1 {
            color: #2d3748;
            font-size: 2.5rem;
            font-weight: 800;
            margin-bottom: 10px;
            background: linear-gradient(135deg, #4facfe 0%, #1dd1a1 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .subtitle {
            color: #718096;
            font-size: 1rem;
            margin-bottom: 10px;
        }

        .song-count {
            display: inline-block;
            background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
            color: white;
            padding: 8px 20px;
            border-radius: 20px;
            font-weight: 600;
            font-size: 0.9rem;
            margin-top: 10px;
        }

        .platform-section {
            margin-bottom: 30px;
        }

        .platform-title {
            color: #4a5568;
            font-size: 1.1rem;
            font-weight: 700;
            margin-bottom: 15px;
            text-align: left;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .platform-title::before {
            content: '';
            width: 4px;
            height: 20px;
            background: linear-gradient(135deg, #4facfe 0%, #1dd1a1 100%);
            border-radius: 2px;
        }

        .button-group {
            display: flex;
            gap: 12px;
            flex-wrap: wrap;
        }

        .stream-btn {
            flex: 1;
            min-width: 140px;
            padding: 18px 30px;
            border: none;
            border-radius: 15px;
            font-weight: 700;
            font-size: 1rem;
            cursor: pointer;
            transition: all 0.3s ease;
            text-decoration: none;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            position: relative;
            overflow: hidden;
        }

        .stream-btn::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            width: 0;
            height: 0;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.3);
            transform: translate(-50%, -50%);
            transition: width 0.6s, height 0.6s;
        }

        .stream-btn:hover::before {
            width: 300px;
            height: 300px;
        }

        .stream-btn:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.2);
        }

        .stream-btn:active {
            transform: translateY(-2px);
        }

        /* 멜론 버튼 */
        .melon-android {
            background: linear-gradient(135deg, #00d084 0%, #00b894 100%);
            color: white;
        }

        .melon-android:hover {
            box-shadow: 0 15px 35px rgba(0, 208, 132, 0.4);
        }

        .melon-iphone {
            background: linear-gradient(135deg, #00b894 0%, #00a078 100%);
            color: white;
        }

        .melon-iphone:hover {
            box-shadow: 0 15px 35px rgba(0, 184, 148, 0.4);
        }

        /* 지니 버튼 */
        .genie-android {
            background: linear-gradient(135deg, #0984e3 0%, #74b9ff 100%);
            color: white;
        }

        .genie-android:hover {
            box-shadow: 0 15px 35px rgba(9, 132, 227, 0.4);
        }

        .genie-iphone {
            background: linear-gradient(135deg, #74b9ff 0%, #0984e3 100%);
            color: white;
        }

        .genie-iphone:hover {
            box-shadow: 0 15px 35px rgba(116, 185, 255, 0.4);
        }

        /* 벅스 버튼 */
        .bugs-btn {
            background: linear-gradient(135deg, #fd79a8 0%, #e84393 100%);
            color: white;
            width: 100%;
        }

        .bugs-btn:hover {
            box-shadow: 0 15px 35px rgba(253, 121, 168, 0.4);
        }

        /* 바이브 버튼 */
        .vibe-btn {
            background: linear-gradient(135deg, #a29bfe 0%, #6c5ce7 100%);
            color: white;
            width: 100%;
        }

        .vibe-btn:hover {
            box-shadow: 0 15px 35px rgba(162, 155, 254, 0.4);
        }

        /* 가이드 섹션 */
        .guide-section {
            margin: 30px 0 20px 0;
        }

        .guide-toggle-btn {
            width: 100%;
            padding: 15px 30px;
            background: linear-gradient(135deg, #f0f4f8 0%, #e2e8f0 100%);
            border: 2px solid #cbd5e0;
            border-radius: 12px;
            font-weight: 600;
            font-size: 1rem;
            color: #4a5568;
            cursor: pointer;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
        }

        .guide-toggle-btn:hover {
            background: linear-gradient(135deg, #e2e8f0 0%, #cbd5e0 100%);
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        }

        .guide-content {
            max-height: 0;
            overflow: hidden;
            transition: max-height 0.5s ease-out;
            margin-top: 20px;
        }

        .guide-content.active {
            max-height: 3000px;
            transition: max-height 0.8s ease-in;
        }

        .guide-item {
            background: #f7fafc;
            border-left: 4px solid #4facfe;
            border-radius: 8px;
            padding: 20px;
            margin-bottom: 15px;
            text-align: left;
        }

        .guide-item.important {
            border-left-color: #fd79a8;
            background: #fff5f7;
        }

        .guide-item h3 {
            color: #2d3748;
            font-size: 1.1rem;
            margin-bottom: 12px;
            font-weight: 700;
        }

        .guide-item ol, .guide-item ul {
            margin-left: 20px;
            color: #4a5568;
            line-height: 1.8;
        }

        .guide-item li {
            margin-bottom: 8px;
        }

        .guide-item a {
            color: #4facfe;
            text-decoration: none;
            font-weight: 600;
        }

        .guide-item a:hover {
            text-decoration: underline;
        }

        .icon {
            font-size: 1.2rem;
        }

        .footer {
            margin-top: 40px;
            padding-top: 30px;
            border-top: 2px solid #e2e8f0;
            color: #a0aec0;
            font-size: 0.85rem;
        }

        .footer-note {
            margin-top: 15px;
            line-height: 1.6;
            color: #cbd5e0;
        }

        /* 반응형 */
        @media (max-width: 480px) {
            .container {
                padding: 40px 25px;
            }

            h1 {
                font-size: 2rem;
            }

            .button-group {
                flex-direction: column;
            }

            .stream-btn {
                width: 100%;
                min-width: auto;
            }
        }

        /* 개발자도구 방지 */
        body {
            -webkit-user-select: none;
            -moz-user-select: none;
            -ms-user-select: none;
            user-select: none;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>🎵 Analog Vol.1</h1>
            <p class="subtitle">[민호특공대] 2025.10.14ver. 음원사이트별 원클릭</p>
            <span class="song-count">전곡 15곡 재생</span>
        </div>

        <!-- 멜론 섹션 -->
        <div class="platform-section">
            <div class="platform-title">
                🍈 멜론
            </div>
            <div class="button-group">
                <a href="https://vo.la/TEIx4gn" class="stream-btn melon-android">
                    <span class="icon">📱</span>
                    <span>안드로이드</span>
                </a>
                <a href="https://lrl.kr/fGkjr" class="stream-btn melon-iphone">
                    <span class="icon">🍎</span>
                    <span>아이폰</span>
                </a>
            </div>
        </div>

        <!-- 지니 섹션 -->
        <div class="platform-section">
            <div class="platform-title">
                💎 지니
            </div>
            <div class="button-group">
                <a href="https://vo.la/ZE9itI" class="stream-btn genie-android">
                    <span class="icon">📱</span>
                    <span>안드로이드</span>
                </a>
                <a href="https://lrl.kr/WIYj" class="stream-btn genie-iphone">
                    <span class="icon">🍎</span>
                    <span>아이폰</span>
                </a>
            </div>
        </div>

        <!-- 벅스 섹션 -->
        <div class="platform-section">
            <div class="platform-title">
                🐞 벅스
            </div>
            <div class="button-group">
                <a href="https://vo.la/PJXLaTx" class="stream-btn bugs-btn">
                    <span class="icon">🎧</span>
                    <span>벅스에서 듣기</span>
                </a>
            </div>
        </div>

        <!-- 바이브 섹션 -->
        <div class="platform-section">
            <div class="platform-title">
                🎵 바이브
            </div>
            <div class="button-group">
                <a href="https://vo.la/yHpkzOI" class="stream-btn vibe-btn">
                    <span class="icon">🎶</span>
                    <span>바이브에서 듣기</span>
                </a>
            </div>
        </div>

        <!-- 스트리밍 가이드 버튼 -->
        <div class="guide-section">
            <button class="guide-toggle-btn" onclick="toggleGuide()">
                <span class="icon">📖</span>
                <span>스트리밍 가이드 보기</span>
            </button>
        </div>

        <!-- 가이드 내용 (기본 숨김) -->
        <div class="guide-content" id="guideContent">
            <div class="guide-item">
                <h3>🍈 멜론 스트리밍 방법</h3>
                <ol>
                    <li>멜론 앱 설치 (미설치 시 <a href="https://www.melon.com/app/index.htm" target="_blank">앱 다운로드</a>)</li>
                    <li>위 버튼 클릭 시 멜론 앱 자동 실행</li>
                    <li>15곡 플레이리스트 자동 재생</li>
                    <li>💡 Tip: 로그인하면 이용권 없이도 30초 미리듣기 가능</li>
                </ol>
            </div>

            <div class="guide-item">
                <h3>💎 지니뮤직 스트리밍 방법</h3>
                <ol>
                    <li>지니뮤직 앱 설치 (미설치 시 <a href="https://www.genie.co.kr/mobile/appdown" target="_blank">앱 다운로드</a>)</li>
                    <li>위 버튼 클릭 시 지니 앱 자동 실행</li>
                    <li>15곡 플레이리스트 자동 재생</li>
                    <li>💡 Tip: 무료 회원도 월 50곡까지 풀 재생 가능</li>
                </ol>
            </div>

            <div class="guide-item">
                <h3>🐞 벅스 스트리밍 방법</h3>
                <ol>
                    <li>벅스 앱 설치 (미설치 시 <a href="https://music.bugs.co.kr/app" target="_blank">앱 다운로드</a>)</li>
                    <li>위 버튼 클릭 시 벅스 앱 자동 실행</li>
                    <li>15곡 플레이리스트 자동 재생</li>
                    <li>💡 Tip: 무료 회원은 30초 미리듣기 가능</li>
                </ol>
            </div>

            <div class="guide-item">
                <h3>🎵 바이브 스트리밍 방법</h3>
                <ol>
                    <li>바이브 앱 설치 (미설치 시 <a href="https://vibe.naver.com/app" target="_blank">앱 다운로드</a>)</li>
                    <li>위 버튼 클릭 시 바이브 앱 자동 실행</li>
                    <li>15곡 플레이리스트 자동 재생</li>
                    <li>💡 Tip: 네이버 플러스 멤버십 가입 시 무제한 이용 가능</li>
                </ol>
            </div>

            <div class="guide-item important">
                <h3>⚠️ 주의사항</h3>
                <ul>
                    <li>앱이 설치되어 있지 않으면 링크가 작동하지 않습니다</li>
                    <li>일부 기기에서는 브라우저 권한 설정이 필요할 수 있습니다</li>
                    <li>각 음원 플랫폼의 이용권에 따라 재생 방식이 다릅니다</li>
                    <li>무료 회원도 미리듣기나 제한적 재생이 가능합니다</li>
                </ul>
            </div>
        </div>

        <div class="footer">
            총 15곡 플레이리스트
            <div class="footer-note">
                버튼을 누르면 해당 앱에서<br>전체 곡이 자동 재생됩니다
            </div>
        </div>
    </div>

    <script>
        // 가이드 토글 함수
        function toggleGuide() {
            const guideContent = document.getElementById('guideContent');
            const toggleBtn = document.querySelector('.guide-toggle-btn');
            
            if (guideContent.classList.contains('active')) {
                guideContent.classList.remove('active');
                toggleBtn.innerHTML = '<span class="icon">📖</span><span>스트리밍 가이드 보기</span>';
            } else {
                guideContent.classList.add('active');
                toggleBtn.innerHTML = '<span class="icon">📖</span><span>가이드 닫기</span>';
            }
        }

        // 개발자도구 방지
        document.addEventListener('contextmenu', e => e.preventDefault());
        
        document.addEventListener('keydown', e => {
            // F12, Ctrl+Shift+I, Ctrl+Shift+J, Ctrl+U 방지
            if (e.key === 'F12' || 
                (e.ctrlKey && e.shiftKey && e.key === 'I') ||
                (e.ctrlKey && e.shiftKey && e.key === 'J') ||
                (e.ctrlKey && e.key === 'U')) {
                e.preventDefault();
                return false;
            }
        });

        // 버튼 클릭 시 애니메이션
        document.querySelectorAll('.stream-btn').forEach(btn => {
            btn.addEventListener('click', function(e) {
                // 클릭 애니메이션 효과
                this.style.transform = 'scale(0.95)';
                setTimeout(() => {
                    this.style.transform = '';
                }, 100);
            });
        });
    </script>
</body>
</html>